import type { BarWeight, RoundingMethod } from '../domain/calculator.types'
interface CalculatorFormProps { rpInput: string; barWeight: BarWeight; roundingMethod: RoundingMethod; onRpInputChange: (value: string) => void; onBarWeightChange: (weight: BarWeight) => void; onRoundingMethodChange: (method: RoundingMethod) => void }
export function CalculatorForm({ rpInput, barWeight, roundingMethod, onRpInputChange, onBarWeightChange, onRoundingMethodChange }: CalculatorFormProps) { return <section className="form-section"><label className="field-label" htmlFor="rp">Tu RP</label><div className="input-wrap"><input className="rp-input" id="rp" inputMode="decimal" onChange={(event) => onRpInputChange(event.target.value)} placeholder="100" value={rpInput} /><span className="input-unit">kg</span></div><div className="control-grid"><div><span className="section-label">Barra</span><div className="segmented">{([20,15] as const).map((weight) => <button aria-pressed={barWeight === weight} className={`choice-button${barWeight === weight ? ' is-active' : ''}`} key={weight} onClick={() => onBarWeightChange(weight)} type="button">{weight} kg</button>)}</div></div><div><span className="section-label">Ajuste</span><div className="segmented"><button aria-pressed={roundingMethod === 'nearest'} className={`choice-button${roundingMethod === 'nearest' ? ' is-active' : ''}`} onClick={() => onRoundingMethodChange('nearest')} type="button">Más cercano</button><button aria-pressed={roundingMethod === 'down'} className={`choice-button${roundingMethod === 'down' ? ' is-active' : ''}`} onClick={() => onRoundingMethodChange('down')} type="button">Hacia abajo</button></div></div></div></section> }
